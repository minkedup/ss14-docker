# ss14-docker

## About The Project

This is a simple repository I setup while looking into how to run a Space
Station 14 server for my friends without needing to install dotnet on my local
machine.

This project allows you to automatically download a release of an SS14 server
and build it into a Dockerfile. See the `BUILD_VERSION` constant in the
`Makefile` to change the build version.
